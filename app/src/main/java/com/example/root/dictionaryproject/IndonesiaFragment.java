package com.example.root.dictionaryproject;import android.net.Uri;import android.os.Bundle;import android.support.v4.app.Fragment;import android.support.v7.widget.LinearLayoutManager;import android.support.v7.widget.RecyclerView;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import com.example.root.dictionaryproject.helper.MyAdapter;import com.example.root.dictionaryproject.helper.MyHelper;import com.example.root.dictionaryproject.helper.MyModel;import java.util.ArrayList;public class IndonesiaFragment extends Fragment {    RecyclerView recyclerView;    MyAdapter adapter;    MyHelper helper;    @Override    public void onViewCreated(View view, Bundle savedInstanceState) {        super.onViewCreated(view, savedInstanceState);        getActivity().setTitle("Indonesia -Inggris");        adapter      = new MyAdapter(getContext());        helper       = new MyHelper(getContext());        recyclerView = (RecyclerView)view.findViewById(R.id.recycler_indonesia);        recyclerView.setLayoutManager(new LinearLayoutManager(getContext()));        recyclerView.setAdapter(adapter);        helper.open();        ArrayList<MyModel> kamus = helper.getAllData();        helper.close();        adapter.addItem(kamus);    }    @Override    public View onCreateView(LayoutInflater inflater, ViewGroup container,                             Bundle savedInstanceState) {        // Inflate the layout for this fragment        return inflater.inflate(R.layout.fragment_indonesia, container, false);    }    public interface OnFragmentInteractionListener {        // TODO: Update argument type and name        void onFragmentInteraction(Uri uri);    }}